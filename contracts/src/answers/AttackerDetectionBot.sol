pragma solidity ^0.8.0;

// Dummy Detection Bot
contract AttackerDetectionBot {

    function handleTransaction(address user, bytes calldata msgData) external {
        // Does nothing, avoiding reverts
    }
}
